import React, { FC } from 'react';
import "./style.less";

const AdminStock: FC = () => {
    
    React.useEffect(() => { return; });

    return (
        <>{"AdminStock"}</>
    );

}

export default AdminStock;