import React, { FC } from 'react';
import "./style.less"

const AddStock: FC = () => {
    
    React.useEffect(() => { return; });

    return (
        <>{"AddStock"}</>
    );

}

export default AddStock;